name: example
on: push
jobs:
   greeting:
     runs-on: ubuntu-latest
     steps:
     - run: echo hello world
   build_skim:
     runs-on: ubuntu-latest
     steps:
       - name: buid
         run: |
           COMPILER=$(root-config --cxx)
           $COMPILER -g -o3 -Wall -Wextras - Wpedantic -o skim skim.cxx
#Comment
